// Basic app test placeholder.
// Full tests will be added following the testing strategy in ADR-006.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO(test): Add proper widget tests with ProviderScope and mocked repos
    expect(true, isTrue);
  });
}
